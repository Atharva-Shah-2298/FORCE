# Dependencies for the FORCE paper experiments.
# FORCE itself ships with DIPY (>= 1.13) as dipy.reconst.force.
dipy>=1.13
numpy
scipy
matplotlib
nibabel
pandas
pyyaml
tqdm

# Optional:
#   ray            -> parallel FORCE fitting (engine="ray")
#   dmri-amico     -> AMICO-NODDI baseline (recovery/ and harmonization/)
